package org.gbif.occurrence.validation.api.model;

/**
 * Represents the validation profile in use.
 * A profile is linked to a different set of validations.
 */
public enum ValidationProfile {
  GBIF_INDEXING_PROFILE
}
